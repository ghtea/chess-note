import { call, spawn, put, takeEvery, takeLatest } from 'redux-saga/effects';

import * as actions from 'store/actions';

export default function* sagaAppearance() {
  // yield takeEvery( actions.appearance.name__DETECT_LANGUAGE, detectLanguage );
}
