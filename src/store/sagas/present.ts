import { call, spawn, put, takeEvery, takeLatest } from "redux-saga/effects";

import * as actionsRoot from "store/actions";
//import detectLanguage from './present/detectLanguage';


export default function* sagaPresent() {
    
    //yield takeEvery( actionsRoot.present.name__DETECT_LANGUAGE, detectLanguage );

    
}